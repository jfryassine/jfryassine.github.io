# Photo Gallery

This project now uses TypeScript for safer component development. To run the project with **npm**:

```bash
npm install
npm start
```

To create a production build:

```bash
npm run build
```
